<div class='navbar navbar-default push'>
  <div class='container'>
    <div class='container'>
      <div class="navbar-header pull-right">
        <% if logged_in? %>
          <div class="navbar-text">Logged in as <strong><%= current_user.name %></strong></div>
          <div class=" pull-right">
            <ul class="nav navbar-nav">
              <li><%= link_to 'Logout', logout_path, class: 'navbar-link' %></li>
            </ul>
		      </div>
        <% end %>
      </div>
      <div class="navbar-header">
        <%= link_to 'Impersona', root_path, class: 'navbar-brand' %>
      </div>
      <ul class="nav navbar-nav">
        <li class="<%= active_nav('index') %>"><%= link_to 'Server Arrays', server_arrays_path, 'data-no-turbolink' => true %></li>
      </ul>
    </div>
  </div>
</div>
